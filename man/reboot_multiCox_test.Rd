% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{reboot_multiCox_test}
\alias{reboot_multiCox_test}
\title{Run multivariate Cox regression with optional bootstrap resampling}
\usage{
reboot_multiCox_test(
  data,
  univ_result,
  logrank_result,
  covariates,
  all_covariates,
  use.bootstrap = TRUE,
  bootstrap = 1,
  bootstrap.freq = 0.25,
  multi.p.cutoff = 0.05
)
}
\arguments{
\item{data}{Data.frame containing survival, REBOOT score and clinical information.
Must contain columns \code{OS}, \code{OS.time}, and \code{score_group}.}

\item{univ_result}{Data.frame generated by \code{reboot_uniCox_test()}.}

\item{logrank_result}{Data.frame generated by \code{reboot_uniCox_model()}.}

\item{covariates}{Character. Vector containing covariates selected for multivariate analysis.}

\item{all_covariates}{Character. Vector containing all available clinical covariates.}

\item{use.bootstrap}{Logical. Whether bootstrap resampling should be used before final model fitting (default: \code{TRUE}).}

\item{bootstrap}{Numeric. Number of bootstrap iterations (default: \code{1}).}

\item{bootstrap.freq}{Numeric. Minimum frequency to retain variables after bootstrap resampling (default: \code{0.25}).}

\item{multi.p.cutoff}{Numeric. P-value used to define prognosis groups in multivariate models (default: \code{0.05}).}
}
\value{
A list containing:
\describe{
\item{table}{Formatted multivariate Cox regression results.}
\item{model}{Final fitted Cox proportional hazards model.}
\item{covariates}{Covariates retained in the final model.}
\item{bootstrap.table}{Bootstrap frequency table (if applicable).}
\item{bootstrap.used}{Logical indicating whether bootstrap was used.}
}
}
\description{
Performs multivariate Cox regression using selected clinical covariates and REBOOT score groups.
Optionally applies bootstrap resampling to identify stable covariates prior to fitting the final model.
}
\keyword{internal}
