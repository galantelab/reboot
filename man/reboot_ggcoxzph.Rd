% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{reboot_ggcoxzph}
\alias{reboot_ggcoxzph}
\title{Customized Schoenfeld residual plots for REBOOT multivariate models}
\usage{
reboot_ggcoxzph(
  fit,
  resid = T,
  se = T,
  df = 4,
  nsmo = 40,
  var,
  point.col = "red",
  point.size = 1,
  point.shape = 19,
  point.alpha = 1,
  caption = NULL,
  ggtheme = survminer::theme_survminer(),
  ...
)
}
\arguments{
\item{fit}{A \code{cox.zph} object generated by \code{survival::cox.zph()}.}

\item{resid}{Logical. Whether to display Schoenfeld residual points (default: \code{TRUE}).}

\item{se}{Logical. Whether to display confidence bands (default: \code{TRUE}).}

\item{df}{Numeric. Degrees of freedom used in spline smoothing (default: \code{4}).}

\item{nsmo}{Numeric. Number of spline smoothing points (default: \code{40}).}

\item{var}{Variables to plot. Can be numeric indices or variable names.}

\item{point.col}{Point color for residuals (default: \code{"red"}).}

\item{point.size}{Numeric. Point size (default: \code{1}).}

\item{point.shape}{Numeric. Point shape (default: \code{19}).}

\item{point.alpha}{Numeric. Point transparency (default: \code{1}).}

\item{caption}{Optional plot caption.}

\item{ggtheme}{ggplot2 theme object (default: \code{survminer::theme_survminer()}).}

\item{...}{Additional arguments passed to \code{ggpubr::ggpar()}.}
}
\value{
A list of \code{ggplot} objects of class \code{"ggcoxzph"}.
}
\description{
Internal plotting helper used by \code{reboot_phplot()} to generate custom Schoenfeld residual plots

This function is based on \code{survminer::ggcoxzph()}, with modifications to:
\itemize{
\item Include global proportional hazards test p-values in plot titles
\item Improve visualization for multivariate Cox regression models
\item Return plot lists compatible with REBOOT plotting workflows
}
}
\keyword{internal}
